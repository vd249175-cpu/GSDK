[package]
name = "graphframework-kernel"
version = "0.1.0"
edition = "2021"
description = "GraphFramework physical rule space: causal DAG, single-flight dispatch, entity admission"
license = "Apache-2.0"

[lib]
name = "graphframework_kernel"
path = "src/lib.rs"
