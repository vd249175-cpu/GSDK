[package]
name = "graphframework-analysis"
version = "0.1.0"
edition = "2021"
description = "Authoritative portable causal analysis: index, folds, paths and structural metrics"
license = "Apache-2.0"

[lib]
name = "graphframework_analysis"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
