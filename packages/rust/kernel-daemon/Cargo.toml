[package]
name = "graphframework-kernel-daemon"
version = "0.1.0"
edition = "2021"
description = "Business-agnostic persistent GraphFramework rule-space host"
license = "Apache-2.0"

[dependencies]
graphframework-kernel = { path = "../kernel" }
graphframework-analysis = { path = "../analysis" }
serde_json = "1.0"
